# integrations-sm

Self-managed dashboards and rules, pushed to Grafana via `gcx` as plain
files (not the Grizzly YAML format used under `integrations/`).

| Folder | Contents |
|--------|----------|
| `duplo-core-dashboard/` | See its own `README.md`. |
